import { TargetPriceFilterPipe } from './target-price-filter.pipe';

describe('TargetPriceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TargetPriceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
