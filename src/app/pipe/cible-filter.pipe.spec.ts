import { CibleFilterPipe } from './cible-filter.pipe';

describe('CibleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CibleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
